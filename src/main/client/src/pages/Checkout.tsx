import React, { FC } from "react"
import { RouteComponentProps } from "@reach/router"

const Checkout: FC<RouteComponentProps> = () => {
  return (
    <div>
      Checkout
    </div>
  )
}

export default Checkout
