import React, { FC } from "react"
import { RouteComponentProps } from "@reach/router"

const UserSettings:FC<RouteComponentProps> = () => {
  return (
    <div>
      User settings
    </div>
  )
}

export default UserSettings
